MYSQL_BUILD_COMMAND="$MYSQL_BUILD_ROOT/bin/mysql-build"

VERSION=${1:?}


case $VERSION in
list)
	$MYSQL_BUILD_COMMAND --definitions
	;;
*)
	$MYSQL_BUILD_COMMAND $VERSION "$MYSQL_ROOT/$VERSION"
	;;
esac
